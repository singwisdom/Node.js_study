### 0819 배운 내용
```
router 사용할때는 vue create 할때 옵션 선택 시 
Manually select 선택 -> Router 선택 , Linter/Formatter 해제

2. npm i axios vue-axios  : 뷰와 axios 연동하는 법 
3. npm install axios : axios만 설치
4. npm run bulid : dist 파일 생성 (웹 배포)

```
```
몽고디비 연결 (몽고 컴파스에서)
mongodb://localhost:27017
2. .env 파일에서 기존 몽고디비 URL 주석처리 하고 mongodb://localhost:(내 아이피주소):27017 이걸로 입력
3. npm run dev

```